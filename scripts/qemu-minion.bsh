#!/usr/bin/env bash
source ${HOME}/.bashrc
################################################################################

declare QNUM="6"
declare QMEM="$((1024*4))"
declare QFIL="${1}"			; shift
declare QSYS="${1}"			; shift

########################################

exec qemu.bsh "${QNUM}" "${QMEM}" "${QFIL}" "${QSYS}" \
	"${@}"

################################################################################
# end of file
################################################################################
